'use strict';

angular.module('auditpagesApp')
  .factory('User', function ($resource) {
    return $resource('/api/use/:id/:controller', {
      id: '@_id'
    },
    {
      changePassword: {
        method: 'PUT',
        params: {
          controller:'password'
        }
      },
      get: {
        method: 'GET',
        params: {
          id:'me'
        }
      }
	  });
  });
